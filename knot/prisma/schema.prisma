generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
}

model Bot {
  id        String   @id @default(uuid())
  userId    String
  name      String
  mood      Int      @default(3)
  createdAt DateTime @default(now())
}
